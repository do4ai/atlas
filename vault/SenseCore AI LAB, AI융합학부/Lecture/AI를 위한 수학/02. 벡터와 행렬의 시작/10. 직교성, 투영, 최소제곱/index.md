---
title: "10. 직교성, 투영, 최소제곱"
---
# 10강. 직교성, 투영, 최소제곱

이 강의의 목표는 공식을 빨리 외우는 것이 아닙니다. 목표는 아래 식을 스스로 설명할 수 있게 되는 것입니다.

$$
a_1^T e = 0
$$

이 식은 말로 풀면 다음 뜻입니다.

> 벡터 $a_1$과 벡터 $e$는 직교한다.

이 문장이 어려운 이유는 한 줄 안에 여러 개념이 한꺼번에 들어 있기 때문입니다.

- $a_1$은 무엇인가?
- $e$는 무엇인가?
- 위에 붙은 $T$는 무엇인가?
- $a_1^T e$는 왜 내적인가?
- 내적이 0이면 왜 직교인가?
- 왜 최소제곱에서는 이런 식이 나오는가?

이 강의는 이 질문을 하나씩, 아주 작은 단계로 나누어 설명합니다.

# 1. 먼저 오늘 쓸 말을 정리하자

수학은 기호가 많습니다. 기호를 뜻도 모른 채 보면 어렵습니다. 그래서 먼저 읽는 법부터 정리합니다.

| 기호 | 읽는 법 | 뜻 |
|---|---|---|
| $v$ | 벡터 $v$ | 숫자를 순서대로 모아 놓은 것, 또는 방향과 크기를 가진 화살표 |
| $v^T$ | $v$ 전치 | 세로 벡터를 가로로 눕힌 것 |
| $u^T v$ | $u$ 전치 곱하기 $v$ | 두 벡터 $u$와 $v$의 내적 |
| $u \cdot v$ | $u$ dot $v$ | 두 벡터의 내적 |
| $\lVert v\rVert$ | $v$의 노름 | 벡터 $v$의 길이 |
| $u^T v=0$ | $u$와 $v$의 내적이 0 | 두 벡터가 직교한다는 조건 |
| $Ax$ | $A$ 곱하기 $x$ | 행렬 $A$의 열벡터들을 $x$의 숫자만큼 섞은 결과 |
| $e=b-Ax$ | 오차 벡터 $e$ | 실제값 $b$와 예측값 $Ax$ 사이에 남은 차이 |

여기서 가장 중요한 것은 이것입니다.

$$
u^T v = u \cdot v
$$

즉 $u^T v$와 $u \cdot v$는 같은 내적을 다르게 쓴 것입니다.

# 2. 벡터는 숫자 묶음이고, 화살표로 볼 수 있다

먼저 벡터부터 다시 봅시다.

예를 들어

$$
v=
\begin{bmatrix}
3 \\
2
\end{bmatrix}
$$

라는 벡터가 있습니다.

이 벡터는 숫자 두 개를 세로로 적은 것입니다.

- 첫 번째 숫자 $3$: 오른쪽으로 3만큼 간다.
- 두 번째 숫자 $2$: 위쪽으로 2만큼 간다.

그래서

$$
\begin{bmatrix}
3 \\
2
\end{bmatrix}
$$

는 평면에서 오른쪽으로 3, 위로 2 가는 화살표로 볼 수 있습니다.

또 다른 예를 봅시다.

$$
\begin{bmatrix}
0 \\
5
\end{bmatrix}
$$

는 오른쪽으로는 가지 않고, 위로만 5만큼 가는 벡터입니다.

$$
\begin{bmatrix}
-2 \\
0
\end{bmatrix}
$$

는 왼쪽으로 2만큼 가고, 위아래로는 움직이지 않는 벡터입니다.

즉 2차원 벡터는 보통 이렇게 읽으면 됩니다.

$$
\begin{bmatrix}
\text{오른쪽 방향 성분} \\
\text{위쪽 방향 성분}
\end{bmatrix}
$$

# 3. 전치란 세로 벡터를 가로로 눕히는 것이다

벡터

$$
a=
\begin{bmatrix}
2 \\
3
\end{bmatrix}
$$

가 있다고 합시다.

이 벡터는 세로로 서 있습니다. 이것을 가로로 눕히면

$$
a^T=
\begin{bmatrix}
2 & 3
\end{bmatrix}
$$

가 됩니다.

여기서 위에 붙은

$$
T
$$

는 transpose, 즉 전치를 뜻합니다.

전치는 어렵게 생각할 필요가 없습니다.

- 세로 벡터를 가로 벡터로 바꾼다.
- 가로 벡터를 세로 벡터로 바꾼다.
- 행렬에서는 행과 열을 서로 바꾼다.

지금은 첫 번째만 기억하면 충분합니다.

$$
\begin{bmatrix}
2 \\
3
\end{bmatrix}
\quad\longrightarrow\quad
\begin{bmatrix}
2 & 3
\end{bmatrix}
$$

# 4. 내적은 같은 자리끼리 곱해서 더하는 계산이다

두 벡터가 있습니다.

$$
a=
\begin{bmatrix}
2 \\
3
\end{bmatrix},
\qquad
e=
\begin{bmatrix}
4 \\
5
\end{bmatrix}
$$

이때

$$
a^T e
$$

를 계산해 봅시다.

먼저

$$
a^T=
\begin{bmatrix}
2 & 3
\end{bmatrix}
$$

입니다. 따라서

$$
a^T e
=
\begin{bmatrix}
2 & 3
\end{bmatrix}
\begin{bmatrix}
4 \\
5
\end{bmatrix}
$$

입니다.

행렬곱 규칙대로 계산하면

$$
a^T e
=2\cdot 4+3\cdot 5
=8+15
=23
$$

입니다.

즉

$$
a^T e
$$

는 같은 자리끼리 곱해서 더한 값입니다.

$$
\begin{bmatrix}
2 \\
3
\end{bmatrix}
\quad\text{와}\quad
\begin{bmatrix}
4 \\
5
\end{bmatrix}
$$

를 비교하면

- 첫 번째 자리끼리 곱한다: $2$ 곱하기 $4$
- 두 번째 자리끼리 곱한다: $3$ 곱하기 $5$
- 두 결과를 더한다: $8$ 더하기 $15$

그래서 답은 $23$입니다.

이 계산을 내적이라고 부릅니다.

$$
a^T e = a \cdot e
$$

중요한 점은 내적의 결과가 벡터가 아니라 숫자라는 것입니다.

$$
a^T e = 23
$$

여기서 $23$은 하나의 숫자입니다.

# 5. 내적은 방향이 얼마나 같은지 알려준다

처음에는 내적이 그냥 같은 자리끼리 곱해서 더하는 계산처럼 보입니다. 그런데 이 계산은 방향에 대한 정보를 담고 있습니다.

아주 쉬운 기준 벡터부터 봅시다.

$$
i=
\begin{bmatrix}
1 \\
0
\end{bmatrix}
$$

이 벡터는 오른쪽 방향입니다. 이제 임의의 벡터

$$
v=
\begin{bmatrix}
3 \\
2
\end{bmatrix}
$$

와 내적해 봅시다.

$$
i^T v
=
\begin{bmatrix}
1 & 0
\end{bmatrix}
\begin{bmatrix}
3 \\
2
\end{bmatrix}
=1\cdot 3+0\cdot 2
=3
$$

결과는 $3$입니다.

그런데 이 $3$은 바로 벡터 $v$가 오른쪽으로 가는 양입니다.

$$
v=
\begin{bmatrix}
3 \\
2
\end{bmatrix}
$$

는 오른쪽으로 3, 위로 2 가는 벡터였습니다. 오른쪽 방향 벡터

$$
i=
\begin{bmatrix}
1 \\
0
\end{bmatrix}
$$

와 내적했더니 오른쪽 방향 성분 $3$이 뽑혀 나온 것입니다.

이번에는 위쪽 방향 벡터를 봅시다.

$$
j=
\begin{bmatrix}
0 \\
1
\end{bmatrix}
$$

계산하면

$$
j^T v
=
\begin{bmatrix}
0 & 1
\end{bmatrix}
\begin{bmatrix}
3 \\
2
\end{bmatrix}
=0\cdot 3+1\cdot 2
=2
$$

이번에는 위쪽 방향 성분 $2$가 뽑혀 나왔습니다.

이 예시에서 내적의 감각을 얻을 수 있습니다.

> 내적은 한 벡터가 다른 벡터 방향으로 얼마나 가고 있는지 재는 값이다.

# 6. 내적값의 부호를 읽는 법

내적값은 양수, 음수, 0이 될 수 있습니다.

## 6.1 같은 방향이면 양수다

오른쪽 방향 벡터

$$
\begin{bmatrix}
1 \\
0
\end{bmatrix}
$$

와 오른쪽으로 5만큼 가는 벡터

$$
\begin{bmatrix}
5 \\
0
\end{bmatrix}
$$

를 내적해 봅시다.

$$
\begin{bmatrix}
1 & 0
\end{bmatrix}
\begin{bmatrix}
5 \\
0
\end{bmatrix}
=5
$$

값이 양수입니다. 두 벡터가 같은 방향을 보고 있기 때문입니다.

## 6.2 반대 방향이면 음수다

이번에는 오른쪽 방향 벡터와 왼쪽으로 가는 벡터를 내적해 봅시다.

$$
\begin{bmatrix}
1 & 0
\end{bmatrix}
\begin{bmatrix}
-5 \\
0
\end{bmatrix}
=-5
$$

값이 음수입니다. 기준은 오른쪽인데, 다른 벡터는 왼쪽으로 가고 있기 때문입니다.

## 6.3 서로 90도면 0이다

오른쪽 방향 벡터와 위쪽 방향 벡터를 내적해 봅시다.

$$
\begin{bmatrix}
1 & 0
\end{bmatrix}
\begin{bmatrix}
0 \\
5
\end{bmatrix}
=1\cdot 0+0\cdot 5
=0
$$

값이 0입니다.

오른쪽 방향과 위쪽 방향은 서로 90도입니다. 한쪽 방향으로는 다른 쪽 성분이 전혀 없습니다.

그래서 내적값은 이렇게 읽습니다.

| 내적값 | 의미 |
|---|---|
| 양수 | 두 벡터가 비슷한 방향을 본다 |
| 음수 | 두 벡터가 반대 방향을 본다 |
| 0 | 한 벡터가 다른 벡터 방향 성분을 갖지 않는다 |

# 7. 직교란 내적이 0인 관계다

두 벡터가 서로 90도로 만나는 것을 직교한다고 합니다.

가장 쉬운 예시는 오른쪽 방향과 위쪽 방향입니다.

$$
a=
\begin{bmatrix}
1 \\
0
\end{bmatrix},
\qquad
e=
\begin{bmatrix}
0 \\
1
\end{bmatrix}
$$

계산하면

$$
a^T e
=
\begin{bmatrix}
1 & 0
\end{bmatrix}
\begin{bmatrix}
0 \\
1
\end{bmatrix}
=1\cdot 0+0\cdot 1
=0
$$

입니다.

그래서

$$
a^T e=0
$$

이라고 쓰면

> $a$와 $e$의 내적이 0이다.

라는 뜻입니다.

그리고 내적이 0이라는 말은

> $e$가 $a$ 방향으로 가진 성분이 없다.

라는 뜻입니다.

그래서 영벡터가 아닌 두 벡터에 대해

$$
a^T e=0
$$

이면 두 벡터는 직교합니다.

단, 한 가지 조심할 점이 있습니다. 영벡터

$$
\begin{bmatrix}
0 \\
0
\end{bmatrix}
$$

는 모든 벡터와 내적하면 0이지만, 방향이 없습니다. 그래서 "각도가 90도다"라고 말하려면 보통 두 벡터가 영벡터가 아니어야 합니다.

# 8. $a_1^T e=0$을 아주 천천히 읽어 보자

이제 처음 목표였던 식을 다시 봅시다.

$$
a_1^T e=0
$$

이 식을 한 번에 이해하려고 하지 말고, 조각으로 나눕니다.

## 8.1 $a_1$은 하나의 벡터다

예를 들어

$$
a_1=
\begin{bmatrix}
2 \\
3
\end{bmatrix}
$$

라고 합시다.

## 8.2 $a_1^T$는 $a_1$을 가로로 눕힌 것이다

$$
a_1^T=
\begin{bmatrix}
2 & 3
\end{bmatrix}
$$

입니다.

## 8.3 $a_1^T e$는 내적이다

예를 들어

$$
e=
\begin{bmatrix}
3 \\
-2
\end{bmatrix}
$$

라고 합시다.

그러면

$$
a_1^T e
=
\begin{bmatrix}
2 & 3
\end{bmatrix}
\begin{bmatrix}
3 \\
-2
\end{bmatrix}
=2\cdot 3+3\cdot (-2)
=6-6
=0
$$

입니다.

따라서

$$
a_1^T e=0
$$

은

> $a_1$과 $e$의 내적이 0이다.

라는 뜻입니다.

그리고 내적이 0이면

> $e$ 안에는 $a_1$ 방향 성분이 없다.

라는 뜻입니다.

그래서

> $a_1$과 $e$는 직교한다.

라고 말합니다.

# 9. 왜 내적 0이 90도를 뜻하는가

앞에서는 숫자 예시로 봤습니다. 이제 공식으로도 확인해 봅시다.

영벡터가 아닌 두 벡터 $u$, $v$ 사이의 각도를 $\theta$라고 하면 내적은 다음처럼 표현됩니다.

$$
u^T v=\|u\|\|v\|\cos\theta
$$

이 식은 이렇게 읽으면 됩니다.

- $\lVert u\rVert$: 벡터 $u$의 길이
- $\lVert v\rVert$: 벡터 $v$의 길이
- $\theta$: 두 벡터 사이의 각도
- $\cos\theta$: 두 벡터가 얼마나 같은 방향을 보는지 나타내는 값

두 벡터가 90도로 만나면

$$
\theta=90^\circ
$$

입니다.

그리고

$$
\cos 90^\circ=0
$$

입니다.

그래서

$$
u^T v
=\|u\|\|v\|\cos 90^\circ
=\|u\|\|v\|\cdot 0
=0
$$

이 됩니다.

반대로 두 벡터가 영벡터가 아닌데

$$
u^T v=0
$$

이라면

$$
\|u\|\|v\|
$$

는 0이 아닙니다. 따라서 0이 되는 이유는

$$
\cos\theta=0
$$

이기 때문입니다. 이때 각도는 90도입니다.

그래서 영벡터가 아닌 두 벡터에 대해

$$
u^T v=0
$$

이면 직교한다고 말합니다.

# 10. 투영은 한 방향 성분만 남기는 것이다

이제 투영을 봅시다.

투영은 쉽게 말해 그림자입니다. 어떤 벡터를 한 방향 위로 내려놓아서, 그 방향 성분만 남기는 것입니다.

먼저 쉬운 예시부터 봅시다.

$$
v=
\begin{bmatrix}
3 \\
2
\end{bmatrix}
$$

라는 벡터가 있습니다.

이 벡터를 x축 방향, 즉 오른쪽 방향 위로 투영하면 어떻게 될까요?

x축 방향 벡터는

$$
u=
\begin{bmatrix}
1 \\
0
\end{bmatrix}
$$

입니다.

벡터 $v$는 오른쪽으로 3, 위로 2 가는 벡터입니다. x축 방향 성분만 남기면 위쪽 성분 2는 사라지고, 오른쪽 성분 3만 남습니다.

그래서 투영 결과는

$$
\operatorname{proj}_u(v)=
\begin{bmatrix}
3 \\
0
\end{bmatrix}
$$

입니다.

원래 벡터는

$$
\begin{bmatrix}
3 \\
2
\end{bmatrix}
$$

였고, 투영 결과는

$$
\begin{bmatrix}
3 \\
0
\end{bmatrix}
$$

입니다.

남은 부분은

$$
r
=
v-\operatorname{proj}_u(v)
=
\begin{bmatrix}
3 \\
2
\end{bmatrix}
-
\begin{bmatrix}
3 \\
0
\end{bmatrix}
=
\begin{bmatrix}
0 \\
2
\end{bmatrix}
$$

입니다.

이 남은 부분

$$
r=
\begin{bmatrix}
0 \\
2
\end{bmatrix}
$$

는 위쪽 방향입니다.

기준 방향

$$
u=
\begin{bmatrix}
1 \\
0
\end{bmatrix}
$$

는 오른쪽 방향입니다.

오른쪽 방향과 위쪽 방향은 직교합니다. 실제로 내적해 보면

$$
u^T r
=
\begin{bmatrix}
1 & 0
\end{bmatrix}
\begin{bmatrix}
0 \\
2
\end{bmatrix}
=0
$$

입니다.

즉 투영하고 남은 부분은 기준 방향과 직교합니다.

# 11. 투영 공식은 왜 이렇게 생겼는가

기준 벡터를

$$
u
$$

라고 하고, 투영할 벡터를

$$
v
$$

라고 합시다.

$v$를 $u$ 방향으로 투영한 결과는 $u$ 방향 위에 있어야 합니다. 그러므로 투영 결과는 어떤 숫자 $c$에 대해

$$
cu
$$

처럼 생겼을 것입니다.

문제는 $c$가 얼마인지 찾는 것입니다.

투영하고 남은 벡터를

$$
r=v-cu
$$

라고 하겠습니다.

투영이 제대로 되었다면, 남은 부분 $r$은 기준 방향 $u$와 직교해야 합니다.

따라서

$$
u^T r=0
$$

이어야 합니다.

여기에

$$
r=v-cu
$$

를 넣으면

$$
u^T(v-cu)=0
$$

입니다.

괄호를 풀면

$$
u^T v-c u^T u=0
$$

입니다.

따라서

$$
c u^T u=u^T v
$$

이고,

$$
c=\frac{u^T v}{u^T u}
$$

입니다.

그래서 투영 공식은

$$
\operatorname{proj}_u(v)=\frac{u^T v}{u^T u}u
$$

입니다.

같은 내적을 점 기호로 쓰면

$$
\operatorname{proj}_u(v)=\frac{v\cdot u}{u\cdot u}u
$$

라고도 씁니다.

단, 기준 벡터 $u$는 영벡터이면 안 됩니다. 영벡터는 방향이 없어서 "그 방향으로 투영한다"는 말 자체가 성립하지 않기 때문입니다.

# 12. 투영 숫자 예시

이번에는 기준 방향이 x축이 아닌 경우를 봅시다.

$$
u=
\begin{bmatrix}
2 \\
1
\end{bmatrix},
\qquad
v=
\begin{bmatrix}
4 \\
3
\end{bmatrix}
$$

라고 합시다.

먼저

$$
u^T v
$$

를 계산합니다.

$$
u^T v
=
\begin{bmatrix}
2 & 1
\end{bmatrix}
\begin{bmatrix}
4 \\
3
\end{bmatrix}
=2\cdot 4+1\cdot 3
=11
$$

다음으로

$$
u^T u
$$

를 계산합니다.

$$
u^T u
=
\begin{bmatrix}
2 & 1
\end{bmatrix}
\begin{bmatrix}
2 \\
1
\end{bmatrix}
=2\cdot 2+1\cdot 1
=5
$$

따라서

$$
\frac{u^T v}{u^T u}
=\frac{11}{5}
$$

입니다.

투영 결과는

$$
\operatorname{proj}_u(v)
=\frac{11}{5}
\begin{bmatrix}
2 \\
1
\end{bmatrix}
=
\begin{bmatrix}
22/5 \\
11/5
\end{bmatrix}
$$

입니다.

남은 벡터는

$$
r
=v-\operatorname{proj}_u(v)
=
\begin{bmatrix}
4 \\
3
\end{bmatrix}
-
\begin{bmatrix}
22/5 \\
11/5
\end{bmatrix}
=
\begin{bmatrix}
-2/5 \\
4/5
\end{bmatrix}
$$

입니다.

정말로 남은 벡터가 $u$와 직교하는지 확인해 봅시다.

$$
u^T r
=
\begin{bmatrix}
2 & 1
\end{bmatrix}
\begin{bmatrix}
-2/5 \\
4/5
\end{bmatrix}
=2\cdot(-2/5)+1\cdot(4/5)
=-4/5+4/5
=0
$$

맞습니다. 투영하고 남은 벡터는 기준 방향과 직교합니다.

# 13. 최소제곱은 가장 가까운 점을 찾는 방법이다

이제 최소제곱으로 넘어갑시다.

우리가 풀고 싶은 식이

$$
Ax=b
$$

라고 합시다.

이 식은 이렇게 읽을 수 있습니다.

> 행렬 $A$의 열벡터들을 잘 섞어서 $b$를 만들 수 있는가?

예를 들어 행렬 $A$의 열벡터가

$$
a_1,\ a_2
$$

이고

$$
A=
\begin{bmatrix}
a_1 & a_2
\end{bmatrix}
$$

라고 합시다.

또

$$
x=
\begin{bmatrix}
x_1 \\
x_2
\end{bmatrix}
$$

라고 하면

$$
Ax=x_1a_1+x_2a_2
$$

입니다.

즉 $Ax$는 $a_1$과 $a_2$를 $x_1$, $x_2$만큼 섞어서 만든 벡터입니다.

$A$의 열벡터들을 여러 비율로 섞어서 만들 수 있는 모든 벡터들의 모음을 열공간이라고 합니다.

그러면

$$
Ax=b
$$

는 이렇게 말할 수 있습니다.

> $b$가 $A$의 열공간 안에 있는가?

만약 $b$가 열공간 안에 있으면 정확한 해가 있습니다. 어떤 $x$를 골라서 정확히 $Ax=b$를 만들 수 있습니다.

하지만 실제 데이터에서는 $b$가 열공간 위에 정확히 놓이지 않는 경우가 많습니다. 그러면 정확한 해가 없습니다.

그럴 때는 "완전히 맞는 답" 대신 "가장 가까운 답"을 찾습니다.

즉

$$
Ax
$$

가

$$
b
$$

에 최대한 가까워지도록 하는

$$
x
$$

를 찾습니다.

이 문제를 최소제곱이라고 합니다.

$$
\min_x \|Ax-b\|^2
$$

이 식은 이렇게 읽습니다.

> $Ax$와 $b$ 사이의 차이의 길이 제곱을 가장 작게 만드는 $x$를 찾아라.

# 14. 오차 벡터는 실제값과 예측값의 차이다

최소제곱에서는 오차 벡터가 중요합니다.

예측값은

$$
Ax
$$

입니다.

실제값은

$$
b
$$

입니다.

그래서 오차 벡터를

$$
e=b-Ax
$$

라고 둡니다.

이 식은 이렇게 읽습니다.

> 실제값에서 예측값을 뺀 것이 오차다.

만약

$$
e=0
$$

이면 오차가 없다는 뜻입니다. 즉 정확히 맞췄다는 뜻입니다.

하지만 정확히 맞출 수 없다면 $e$가 완전히 0이 될 수 없습니다. 그때는 $e$의 길이가 가장 작아지도록 해야 합니다.

# 15. 가장 가까운 점에서는 오차가 열공간과 직교한다

이제 최소제곱의 가장 중요한 문장을 봅시다.

> 가장 가까운 점에서는 오차 벡터가 열공간과 직교한다.

이 말이 왜 맞는지 쉬운 그림으로 생각해 봅시다.

바닥 위에 어떤 점들이 있고, 우리는 바닥 위에서만 움직일 수 있다고 합시다. 그런데 목표점 $b$는 바닥 위가 아니라 공중에 떠 있습니다.

바닥 위에서 목표점 $b$에 가장 가까운 점은 어디일까요?

바로 목표점에서 바닥으로 수직으로 내려온 점입니다.

왜 수직일까요?

만약 목표점과 바닥 위의 점을 잇는 선이 비스듬하다면, 바닥 위에서 조금 더 움직여서 거리를 줄일 수 있습니다. 가장 가까운 점에서는 더 이상 바닥 방향으로 줄일 수 있는 성분이 없어야 합니다.

그래서 남은 오차는 바닥과 수직입니다.

선형대수에서는 이 "바닥" 역할을 하는 것이 열공간입니다.

- 예측값 $Ax$는 열공간 안에 있습니다.
- 실제값 $b$는 열공간 밖에 있을 수 있습니다.
- 오차 $e=b-Ax$는 예측값에서 실제값으로 가는 남은 차이입니다.
- 가장 가까운 예측값을 찾으면, 오차 $e$는 열공간과 직교합니다.

즉 최소제곱 해에서는

$$
e=b-Ax
$$

가 행렬 $A$의 모든 열벡터와 직교합니다.

# 16. 열공간과 직교한다는 말은 각 열벡터와 직교한다는 말이다

행렬 $A$의 열벡터가

$$
a_1,\ a_2,\ \dots,\ a_n
$$

이라고 합시다.

그러면

$$
A=
\begin{bmatrix}
a_1 & a_2 & \cdots & a_n
\end{bmatrix}
$$

입니다.

오차 벡터 $e$가 열공간과 직교한다는 말은, 특히 $e$가 열공간을 만드는 모든 기본 방향과 직교한다는 뜻입니다.

즉

$$
a_1^T e=0
$$

이고,

$$
a_2^T e=0
$$

이고,

계속해서

$$
a_n^T e=0
$$

입니다.

이것을 한 줄로 쓰면

$$
a_1^T e=0,\quad
a_2^T e=0,\quad
\cdots,\quad
a_n^T e=0
$$

입니다.

각 식의 뜻은 같습니다.

> 각 열벡터와 오차 벡터의 내적이 0이다.

즉

> 오차 벡터는 행렬 $A$가 움직일 수 있는 모든 방향과 직교한다.

라는 뜻입니다.

# 17. 여러 개의 직교 조건을 $A^T e=0$으로 압축한다

방금 조건을 다시 봅시다.

$$
a_1^T e=0,\quad
a_2^T e=0,\quad
\cdots,\quad
a_n^T e=0
$$

이 식을 매번 길게 쓰기는 불편합니다.

행렬 전치를 이용하면 한 번에 쓸 수 있습니다.

$$
A=
\begin{bmatrix}
a_1 & a_2 & \cdots & a_n
\end{bmatrix}
$$

이면

$$
A^T=
\begin{bmatrix}
a_1^T \\
a_2^T \\
\vdots \\
a_n^T
\end{bmatrix}
$$

입니다.

따라서

$$
A^T e
=
\begin{bmatrix}
a_1^T \\
a_2^T \\
\vdots \\
a_n^T
\end{bmatrix}
e
=
\begin{bmatrix}
a_1^T e \\
a_2^T e \\
\vdots \\
a_n^T e
\end{bmatrix}
$$

입니다.

그러므로

$$
A^T e=0
$$

은 사실 다음 조건들을 한꺼번에 쓴 것입니다.

$$
\begin{bmatrix}
a_1^T e \\
a_2^T e \\
\vdots \\
a_n^T e
\end{bmatrix}
=
\begin{bmatrix}
0 \\
0 \\
\vdots \\
0
\end{bmatrix}
$$

즉

$$
A^T e=0
$$

은

> 오차 $e$가 $A$의 모든 열벡터와 직교한다.

라는 말을 짧게 쓴 것입니다.

여기서 오른쪽의 $0$은 보통 하나의 숫자 0처럼 쓰지만, 실제로는 모든 성분이 0인 영벡터입니다.

# 18. 정규방정식은 직교 조건을 정리한 것이다

최소제곱에서는 오차를

$$
e=b-Ax
$$

라고 두었습니다.

가장 가까운 점에서는 오차가 열공간과 직교하므로

$$
A^T e=0
$$

입니다.

여기에

$$
e=b-Ax
$$

를 넣습니다.

$$
A^T(b-Ax)=0
$$

괄호를 풀면

$$
A^T b-A^T A x=0
$$

입니다.

양변을 정리하면

$$
A^T A x=A^T b
$$

가 됩니다.

이 식을 정규방정식이라고 합니다.

정규방정식은 갑자기 나온 공식이 아닙니다. 아래 과정을 압축한 결과입니다.

1. 정확히 $Ax=b$를 만들 수 없을 수 있다.
2. 그러면 $Ax$가 $b$에 가장 가까워지도록 한다.
3. 가장 가까운 점에서는 오차 $e=b-Ax$가 열공간과 직교한다.
4. 열공간과 직교한다는 말은 $A$의 모든 열벡터와 직교한다는 말이다.
5. 그 조건을 쓰면 $A^T e=0$이다.
6. $e=b-Ax$를 넣으면 $A^T(b-Ax)=0$이다.
7. 정리하면 $A^T A x=A^T b$이다.

# 19. 선형회귀와 연결하기

선형회귀도 같은 생각을 사용합니다.

데이터 점들이 하나의 직선 위에 정확히 놓이지 않는다고 합시다. 그러면 모든 점을 완벽하게 지나는 직선은 없을 수 있습니다.

그래도 우리는 가장 잘 맞는 직선을 찾고 싶습니다.

이때 각 데이터의 실제값과 예측값 사이에는 오차가 생깁니다. 선형회귀는 이 오차들의 제곱합이 가장 작아지는 직선을 찾습니다.

즉 선형회귀는 최소제곱 문제입니다.

그리고 최소제곱 문제의 핵심 조건은

$$
A^T(b-Ax)=0
$$

입니다.

이 식은 말로 풀면 다음과 같습니다.

> 더 이상 줄일 수 없는 최선의 예측에서는, 남은 오차가 모델이 움직일 수 있는 방향들과 직교한다.

# 20. 예제 1: 두 벡터가 직교하는지 확인하기

문제: 다음 두 벡터가 직교하는지 확인하라.

$$
u=
\begin{bmatrix}
1 \\
2
\end{bmatrix},
\qquad
v=
\begin{bmatrix}
2 \\
-1
\end{bmatrix}
$$

풀이:

$$
u^T v
=
\begin{bmatrix}
1 & 2
\end{bmatrix}
\begin{bmatrix}
2 \\
-1
\end{bmatrix}
=1\cdot 2+2\cdot (-1)
=2-2
=0
$$

내적이 0입니다. 따라서 두 벡터는 직교합니다.

# 21. 예제 2: $a_1^T e=0$ 해석하기

문제: 다음 식의 뜻을 설명하라.

$$
a_1^T e=0
$$

풀이:

$a_1^T e$는 벡터 $a_1$과 벡터 $e$의 내적입니다.

따라서

$$
a_1^T e=0
$$

은

> $a_1$과 $e$의 내적이 0이다.

라는 뜻입니다.

내적이 0이라는 말은

> $e$가 $a_1$ 방향 성분을 갖지 않는다.

라는 뜻입니다.

그래서 영벡터가 아닌 경우에는

> $a_1$과 $e$가 직교한다.

라고 말할 수 있습니다.

# 22. 예제 3: 투영하고 남은 벡터가 직교하는지 확인하기

문제: 벡터

$$
v=
\begin{bmatrix}
3 \\
1
\end{bmatrix}
$$

를

$$
u=
\begin{bmatrix}
1 \\
0
\end{bmatrix}
$$

방향으로 투영하라. 그리고 남은 벡터가 $u$와 직교하는지 확인하라.

풀이:

$u$는 x축 방향입니다. 따라서 $v$에서 x축 방향 성분만 남기면

$$
\operatorname{proj}_u(v)=
\begin{bmatrix}
3 \\
0
\end{bmatrix}
$$

입니다.

남은 벡터는

$$
r
=
v-\operatorname{proj}_u(v)
=
\begin{bmatrix}
3 \\
1
\end{bmatrix}
-
\begin{bmatrix}
3 \\
0
\end{bmatrix}
=
\begin{bmatrix}
0 \\
1
\end{bmatrix}
$$

입니다.

내적을 계산하면

$$
u^T r
=
\begin{bmatrix}
1 & 0
\end{bmatrix}
\begin{bmatrix}
0 \\
1
\end{bmatrix}
=0
$$

입니다. 따라서 남은 벡터 $r$은 기준 방향 $u$와 직교합니다.

# 23. 예제 4: 정규방정식이 나오는 순서 말하기

문제: 최소제곱에서 왜

$$
A^TAx=A^Tb
$$

가 나오는지 설명하라.

풀이:

최소제곱에서는 예측값 $Ax$가 실제값 $b$에 가장 가까워지도록 합니다.

오차 벡터를

$$
e=b-Ax
$$

라고 두면, 가장 가까운 점에서는 오차가 열공간과 직교합니다.

그래서

$$
A^T e=0
$$

입니다.

여기에

$$
e=b-Ax
$$

를 넣으면

$$
A^T(b-Ax)=0
$$

입니다.

정리하면

$$
A^T b-A^T A x=0
$$

이고,

$$
A^T A x=A^T b
$$

가 됩니다.

# 24. 한 줄씩 다시 요약하기

- 벡터는 숫자 묶음이고, 화살표로 볼 수 있다.
- 전치 $a^T$는 세로 벡터 $a$를 가로로 눕힌 것이다.
- $a^T e$는 두 벡터 $a$와 $e$의 내적이다.
- 내적은 같은 자리끼리 곱해서 더한 값이다.
- 내적은 한 벡터가 다른 벡터 방향으로 얼마나 가고 있는지 알려준다.
- 내적이 0이면 한 벡터가 다른 벡터 방향 성분을 갖지 않는다는 뜻이다.
- 영벡터가 아닌 두 벡터의 내적이 0이면 두 벡터는 직교한다.
- 투영은 한 방향 성분만 남기는 것이다.
- 투영하고 남은 벡터는 기준 방향과 직교한다.
- 최소제곱은 정확한 해가 없을 때 가장 가까운 해를 찾는 방법이다.
- 최소제곱 해에서는 오차 $e=b-Ax$가 열공간과 직교한다.
- 열공간과 직교한다는 말은 $A$의 모든 열벡터와 직교한다는 말이다.
- 이 조건을 압축하면 $A^T e=0$이다.
- $e=b-Ax$를 넣으면 $A^T(b-Ax)=0$이다.
- 정리하면 정규방정식 $A^TAx=A^Tb$가 된다.

# 25. 스스로 점검

1. 벡터

$$
\begin{bmatrix}
3 \\
2
\end{bmatrix}
$$

를 말로 설명할 수 있는가?

2. 다음 전치를 계산할 수 있는가?

$$
\begin{bmatrix}
4 \\
7
\end{bmatrix}^T
$$

3. 다음 내적을 계산할 수 있는가?

$$
\begin{bmatrix}
2 & 3
\end{bmatrix}
\begin{bmatrix}
5 \\
1
\end{bmatrix}
$$

4. $a_1^T e=0$이 왜 $a_1$과 $e$의 내적이 0이라는 뜻인지 설명할 수 있는가?

5. 내적이 0이면 왜 직교라고 말하는지 설명할 수 있는가?

6. 투영하고 남은 벡터가 왜 기준 방향과 직교하는지 설명할 수 있는가?

7. 최소제곱에서 오차 벡터 $e=b-Ax$가 왜 열공간과 직교해야 하는지 설명할 수 있는가?

8. 다음 식이 무엇을 압축한 것인지 설명할 수 있는가?

$$
A^T e=0
$$

9. 다음 정규방정식이 어떤 순서로 나오는지 말할 수 있는가?

$$
A^TAx=A^Tb
$$
